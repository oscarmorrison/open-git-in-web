echo "Opening git repo"
giturl=$(git config --get remote.origin.url)

if [[ $giturl == *"github"* ]]
then
  open $giturl
else
	giturl=${giturl/\.git}
	giturl=${giturl//[:]/\/}
	giturl=${giturl/\git@}
	protocol="http://"
	giturl="$protocol$giturl"
	open $giturl
fi
